# Where is the (java) selenium server running?
default_remote=http://127.0.0.1:4444

# Where should the selenium server send the browser to test the application?
server_scheme=http
server_hostname=localhost
server_port=8383

[FIREFOX]
remote=${default_remote}