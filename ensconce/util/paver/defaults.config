[build]
strict = true
package =
release = 1

build_dir = %(cwd)s/build
rpm_topdir = %(cwd)s/build
dist_dir = %(cwd)s/dist

rpm_resources_dir = %(cwd)s/package-support/rpm
spec_file = %(rpm_resources_dir)s/%(package)s.spec